# Copy this file to .env and edit values before running
PORT=3000
JWT_SECRET=please-change-me-to-a-very-strong-secret
JWT_EXPIRES_IN=7d
CORS_ORIGIN=*            # set to your frontend origin in production
MAX_PERSISTED_LOCATIONS=5000
PORTION_PERSIST_EVERY=5   # persist every N updates per socket (reduces disk writes)